# Public storefront URL, used for canonical links and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=https://ateliernord.com

# Hosted checkout the cart hands off to. The cart id is appended as ?cart=<id>.
NEXT_PUBLIC_CHECKOUT_URL=https://checkout.ateliernord.com/c

# Optional newsletter provider endpoint (e.g. an ESP list webhook).
NEWSLETTER_WEBHOOK_URL=
